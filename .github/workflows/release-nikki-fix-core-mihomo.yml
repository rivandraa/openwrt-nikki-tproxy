name: release nikki fixed core Mihomo

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.arch }}-${{ matrix.branch }} build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - aarch64_generic
          - x86_64
        branch:
          - openwrt-24.10
          - openwrt-25.12

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build package
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: ${{ matrix.arch }}-${{ matrix.branch }}
          FEEDNAME: nikki
          PACKAGES: luci-app-nikki
          NO_REFRESH_CHECK: true

      - name: Extract package versions
        id: extract_version
        run: |
          set -e
          PKG_DIR="bin/packages/${{ matrix.arch }}/nikki"

          # luci-app-nikki
          PKG_FILE_LUCI=$(find "$PKG_DIR" -name 'luci-app-nikki_*.ipk' | head -n 1)
          [ -z "$PKG_FILE_LUCI" ] && echo "❌ luci-app-nikki not found!" && exit 1
          VERSION_LUCI=$(basename "$PKG_FILE_LUCI" | sed -E 's/^luci-app-nikki_([^_]+)_.*\.ipk/\1/')

          # nikki
          PKG_FILE_NIKKI=$(find "$PKG_DIR" -name 'nikki_*.ipk' | head -n 1)
          [ -z "$PKG_FILE_NIKKI" ] && echo "❌ nikki not found!" && exit 1
          VERSION_NIKKI=$(basename "$PKG_FILE_NIKKI" | sed -E 's/^nikki_([^_]+)_.*\.ipk/\1/')

          echo "version_luci=$VERSION_LUCI" >> $GITHUB_OUTPUT
          echo "version_nikki=$VERSION_NIKKI" >> $GITHUB_OUTPUT

      - name: Create release metadata
        id: release_meta
        run: |
          set -e
          
          VER_LUCI_RAW="${{ steps.extract_version.outputs.version_luci }}"     
          VER_NIKKI_RAW="${{ steps.extract_version.outputs.version_nikki }}"   
          ARCH="${{ matrix.arch }}"
          BRANCH="${{ matrix.branch }}"

          echo "🧪 Input:"
          echo "VER_LUCI_RAW = $VER_LUCI_RAW"
          echo "VER_NIKKI_RAW = $VER_NIKKI_RAW"

          # Bersihkan tanggal di depan versi nikki (biarkan sisanya apa adanya)
          VER_NIKKI_CLEAN=$(echo "$VER_NIKKI_RAW" | sed -E 's/^[0-9]{4}-[0-9]{2}-[0-9]{2}~//')

          TAG_NAME="Nikki-TProxy-$VER_LUCI_RAW"
          HASH_TAG="Nikki_TProxy_$(echo "$VER_LUCI_RAW" | sed 's/\./_/g; s/-/_/g')"

          # Nama zip full → langsung gabungkan versi luci raw + nikki clean
          ZIP_NAME="Nikki_TProxy_${VER_LUCI_RAW}_Nikki_${VER_NIKKI_CLEAN}_${ARCH}"

          echo "tagname=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "zipname=$ZIP_NAME" >> $GITHUB_OUTPUT
          echo "relname=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "hashtag=$HASH_TAG" >> $GITHUB_OUTPUT

          echo ""
          echo "✅ Generated:"
          echo "🔖 TAG_NAME = $TAG_NAME"
          echo "📦 ZIP_NAME = $ZIP_NAME.zip"
          echo "🏷️ HASH_TAG = #$HASH_TAG"

      - name: Debug outputs
        run: |
          echo "TAG_NAME  = ${{ steps.release_meta.outputs.tagname }}"
          echo "ZIP_NAME  = ${{ steps.release_meta.outputs.zipname }}"
          echo "REL_NAME  = ${{ steps.release_meta.outputs.relname }}"
          echo "HASH_TAG  = ${{ steps.release_meta.outputs.hashtag }}"

      - name: Compress .ipk files to ZIP
        run: |
          mkdir -p output
          cp bin/packages/${{ matrix.arch }}/nikki/*.ipk output/
          zip -j "${{ steps.release_meta.outputs.zipname }}.zip" output/*.ipk

      - name: Upload ZIP to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release_meta.outputs.tagname }}
          name: ${{ steps.release_meta.outputs.relname }}
          files: ${{ steps.release_meta.outputs.zipname }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}

      - name: Upload ZIP to Telegram Channel
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          set -e
      
          ORIGINAL_FILE="${{ steps.release_meta.outputs.zipname }}.zip"
          VER_LUCI="${{ steps.extract_version.outputs.version_luci }}"
          VER_NIKKI="${{ steps.extract_version.outputs.version_nikki }}"
          ARCH="${{ matrix.arch }}"
      
          echo "📦 Asli: $ORIGINAL_FILE"
      
          CAPTION="🎉 <b>Release</b> #${{ steps.release_meta.outputs.hashtag }}
          
          🆎 luci-app-nikki : <code>$VER_LUCI</code>
          🧩 nikki : <code>$VER_NIKKI</code>
          📦 Arch : <code>$ARCH</code>
          🌿 Branch : <code>${{ matrix.branch }}</code>
          🚀 Core : Mihomo"
      
          # Tentukan nama file aman untuk Telegram
          FILENAME="$(basename "$ORIGINAL_FILE")"
          MAX_LEN=60
          if [ ${#FILENAME} -gt $MAX_LEN ]; then
            EXT="${FILENAME##*.}"
            NAME="${FILENAME%.*}"
            SAFE_NAME="${NAME:0:$MAX_LEN}.${EXT}"
          else
            SAFE_NAME="$FILENAME"
          fi

          echo "📂 Nama upload: $SAFE_NAME"
      
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F "document=@${ORIGINAL_FILE};filename=${SAFE_NAME}" \
            -F caption="$CAPTION" \
            -F parse_mode="HTML"

      - name: Create GitHub Release (rivandraa/nikki_tproxy) WITHOUT file
        uses: softprops/action-gh-release@v2
        with:
          repository: rivandraa/nikki_tproxy
          tag_name: ${{ steps.release_meta.outputs.tagname }}
          name: "Release ${{ steps.release_meta.outputs.tagname }}"  # Tanpa emoji
        env:
          GITHUB_TOKEN: ${{ secrets.TARGET_REPO_PAT }}
